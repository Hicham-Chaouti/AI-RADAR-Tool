// TODO (Phase 6): Implement use case detail modal
/** Modal showing full use case detail with clickable source URL. */
export default function UseCaseDetailModal() {
    return <div className="fixed inset-0 bg-black/50"><p className="text-white">Phase 6 — Detail modal</p></div>
}
